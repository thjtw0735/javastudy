/*package baseball;

public class Test {

	public void nomatch(){
		Game game = new Game(479);
		
		assertNoMatch(game.guess(123));
		assertNoMatch(game.guess(568));
	}
	
	public void assertNoMacth(Score s ){
		assertThat(s.strikes()).isEqualTo(0);
		assertThat(s.balls()).isEqualTo(0);
		
	}
	
	
}
*/